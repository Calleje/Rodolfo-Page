import React, { useState } from "react";

import "../Hojas-de-estilo/Apuesta.css";

const Apuesta = ({ apuesta }) => {
  //   const [total, setTotal] = useState("asdf");

  return (
    <div>
      <div>El total es: {apuesta} </div>
    </div>
  );
};

export default Apuesta;
